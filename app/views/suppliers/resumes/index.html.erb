<%= render 'suppliers/shared/search', title: '我的简历', url: suppliers_resumes_path, placeholder: '被推荐人、标签、职位名称', search_fields: 'candidate_name_or_jobs_title_or_tags_name_cont' %>
<%= render 'list', resumes: @resumes %>
<%= render 'shared/paginate', objs: @resumes %>
<%= link_to '上传简历', new_suppliers_resume_path, class: 'btn btn-primary' %>
