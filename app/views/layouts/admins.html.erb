<!DOCTYPE html>
<html lang="en">
  <%= render partial: "/shared/head" %>
  <body>
    <%= render partial: "/shared/navbar" %>

    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-3 col-md-2 sidebar">
          <ul class="nav nav-sidebar">

            <% active = controller.class == Admins::ResumesController ? "active" : "" %>
            <li class="<%= active %>"><%= link_to "简历库", admins_resumes_path %></li>
            <% active = controller.class == Admins::UsersController ? "active" : "" %>
            <li class="<%= active %>">
              <%= link_to '用户列表', admins_users_path %>
            </li>
            <li><%= link_to "公司列表", 'javascript:void(0);' %></li>

            <li><a href="javascript:void(0);">职位</a></li>
          </ul>
        </div>
        <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
<!-- http://getbootstrap.com/getting-started/ -->

