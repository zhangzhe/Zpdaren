<% if params[:job_id] %>
  <%= render 'job_deliveries_nav_tabs', deliveries: @deliveries, job: @job %>
<% else %>
  <%= render 'nav_tabs', deliveries: @deliveries %>
<% end %>
<%= render 'shared/popover' %>
