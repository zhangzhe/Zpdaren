<% if params[:job_id] %>
  <%= render 'job', recommended_deliveries: @recommended_deliveries, approved_deliveries: @approved_deliveries, job: @job %>
<% else %>
  <%= render 'all', deliveries: @deliveries %>
<% end %>
<%= render 'shared/popover' %>
