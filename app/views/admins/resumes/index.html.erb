<%= render 'search' %>
<%= render 'list', resumes: @resumes %>
<div class='text-center'>
  <%= will_paginate @resumes, renderer: BootstrapPagination::Rails, previous_label: '上一页', next_label: '下一页' %>
</div>
